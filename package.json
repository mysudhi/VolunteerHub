{
  "name": "volunteerhub",
  "private": true,
  "version": "0.1.0",
  "description": "VolunteerHub monorepo scaffold",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "dev": "npm run dev -ws --if-present",
    "lint": "npm run lint -ws --if-present",
    "test": "npm run test -ws --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
